# LLM Playground Studio

A Streamlit workspace for experimenting with LLM prompts, tokenizers, embeddings, model comparisons, and analytics.

## Setup

```powershell
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
streamlit run app.py
```
